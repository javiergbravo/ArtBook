<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <ImageView
        android:id="@+id/artRowImageView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_launcher_background" />

    <TextView
        android:id="@+id/artRowNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_marginBottom="14dp"
        android:textSize="28sp"
        app:layout_constraintStart_toEndOf="@id/artRowImageView"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Art Name" />

    <TextView
        android:id="@+id/artRowArtistNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_marginBottom="14dp"
        android:textSize="20sp"
        app:layout_constraintStart_toEndOf="@id/artRowImageView"
        app:layout_constraintTop_toBottomOf="@id/artRowNameText"
        tools:text="Artist Name" />

    <TextView
        android:id="@+id/artRowYearNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_marginBottom="14dp"
        android:textSize="20sp"
        app:layout_constraintStart_toEndOf="@id/artRowImageView"
        app:layout_constraintTop_toBottomOf="@id/artRowArtistNameText"
        tools:text="Year" />

</androidx.constraintlayout.widget.ConstraintLayout>